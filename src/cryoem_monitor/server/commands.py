# Prometheus: cd into directory
# ./prometheus --config.file=prometheus.yml --web.listen-address=:3000

# FastAPI
# FastAPI run src/cryoem_monitor/server/prometheus  .py

# Murfey: cd into directory
# ./bin/grafana server
